import { StyleSheet } from "react-native";

export const styleHeadbar = StyleSheet.create({
    container:{
        flex: 0.5,
        backgroundColor: "#FCD46E",
        alignItems: "center",
        justifyContent: "center"
    },
})