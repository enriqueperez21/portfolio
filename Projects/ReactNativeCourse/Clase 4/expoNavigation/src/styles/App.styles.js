import { StyleSheet } from "react-native";

export const appStyle = StyleSheet.create({
    container:{
        flex: 1,
        backgroundColor: '#fff',
        justifyContent: "center",
        alignItems: 'center'
    }
})

//Propiedades de estilos.
/*
Css -> display: grid/flex/etc

React Native: Flex -> FlexBox

*/